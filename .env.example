# Copie este arquivo para .env e preencha com os valores reais.
# O .env de verdade NUNCA deve ser commitado (já está no .gitignore).

DB_HOST=localhost
DB_PORT=5432
DB_NAME=praxis
DB_USER=praxis_user
DB_PASSWORD=troque_esta_senha
